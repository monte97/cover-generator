# Template Examples

HTML + PNG previews of all 16 templates. Generated with `make export`.

Open `index.html` in a browser for an interactive gallery, or browse the PNGs directly.

## Regenerate

```bash
make export                        # HTML + PNG (requires Playwright)
node export-templates.js --html-only  # HTML only
```
